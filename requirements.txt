# Phase 1 — Document Ingestion & Chunking
numpy
pypdf

# Phase 2 — Embeddings & Vector Store
chromadb
requests
sentence-transformers   # local embedding model (also used by ingest.py --sentence-transformers)
# nomic-embed-text itself needs no Python package — it runs via a local
# Ollama daemon (`ollama pull nomic-embed-text`); src/embeddings/nomic_embedder.py
# just calls its HTTP API with `requests`.

# Phase 3 — Retrieval
rank_bm25   # BM25 keyword search for hybrid search
# The cross-encoder re-ranker (cross-encoder/ms-marco-MiniLM-L-6-v2) reuses
# the sentence-transformers install above — no separate package needed,
# just a separate model download on first use (src/retrieval/reranker.py).

# Phase 4 — RAG Pipeline: LangChain implementation
langchain-core
langchain-ollama   # ChatOllama; local generation via the Ollama daemon (llama3.1:8b)
tiktoken           # approximate, model-agnostic token counting for context budgeting

# Phase 5 — RAG Pipeline: LlamaIndex implementation
llama-index-core
llama-index-llms-ollama              # LlamaIndex's Ollama LLM wrapper
llama-index-vector-stores-chroma     # points a VectorStoreIndex at an existing Chroma collection

# Dev-only, used to generate the sample PDF in data/raw/ and run tests/
# reportlab
# pytest
